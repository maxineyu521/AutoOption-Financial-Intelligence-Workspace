"""Top-level `Scripts` package.

Explicit package marker so the project stops relying on PEP 420 implicit
namespace packages. This makes tooling (mypy, pyinstaller, wheel builds)
and test collection behave deterministically.
"""
